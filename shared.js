/* ===== 앱스 스크립트 주소 =====================================
   ask.html · review.html · story.html 이 공통으로 쓰는 기본 주소입니다.
   앱스 스크립트를 "새 배포"로 다시 만들어 주소가 바뀌면
   아래 따옴표 안의 주소만 새 것으로 바꿔 이 파일 하나만 다시 올리면 됩니다.
   (배포 관리 → 연필 → 새 버전으로 하면 주소는 바뀌지 않습니다)

   이미 보낸 링크는 이 값이 바뀌어도 계속 열립니다 — 링크 끝에 붙는
   ~배포ID 가 있으면 그 배포ID를 대신 쓰기 때문입니다. 이 파일은
   ~배포ID 가 없는 "짧은 링크"를 열 때만 쓰이는 기본값입니다.
   ============================================================ */
const GS_URL = 'https://script.google.com/macros/s/AKfycbyULnOlwGCrwEjeqgocZhZjh8pMazeaEvl8H78vd7RV61aX9HKq0a6tS_0KWZcf7Ufjhg/exec';
